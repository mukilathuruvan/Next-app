import React from "react";
import { Container } from "@mui/material";
const Sample = () => {
  return (
    <Container sx={{ backgroundColor: "greenyellow" }} fixed maxWidth="md">
      Sample
    </Container>
  );
};

export default Sample;
